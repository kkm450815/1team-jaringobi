# 모든 환경에서 사용하는 공개 URL — 메일/OAuth redirect 의 기준점.
# Capacitor 안드로이드 webview 는 window.location.origin 이 https://localhost
# 같은 무용지물 값이므로, redirectTo 는 항상 이 값을 쓰도록 강제한다.
VITE_PUBLIC_URL=https://1team-jaringobi.vercel.app
